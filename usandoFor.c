#include <stdio.h>

int main() {

    for (int numeros = 1; numeros < 101; numeros++) { // Loop de 1 a 100
        printf("%d\n", numeros); // Imprime o número atual
    }
    
    return 0; // Retorna 0 para indicar que o programa terminou corretamente
}